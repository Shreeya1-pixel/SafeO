# SafeO FastAPI backend — copy to .env and fill in (never commit .env)
#
# Optional: tiered LLM augmentation via OpenRouter (disabled by default).
SECUREC_ENABLE_LLM_AUGMENTATION=false
OPENROUTER_API_KEY=
SECUREC_OPENROUTER_MODEL=openai/gpt-4.1-mini
